[project]
name = "fluxmod-bot"
version = "0.1.0"
description = "FluxMod a Fluxer bot to help with auto moderation."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
	"aiofiles>=25.1.0",
	"colorama",
	"fluxer.py",
	"pymongo",
	"pyright>=1.1.408",
	"python-dotenv",
]
